import React from 'react';

import { Button } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Button variant="contained" color="primary">
        Hello Material-UI
      </Button>
    </div>
  );
};

export default Home;
